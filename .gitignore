# Dependencies
node_modules/

# Local dev's default DATA_PATH — items/favourites as files, and
# optionally its own nested git repo (see designs/file-backed-storage.md)
backend/data/

# Environment
.env
.env.local
.env.secret

# Local satellite address book — hand-edited, never committed
backend/satellites.json

# Terraform
infra/.terraform/
infra/.terraform.lock.hcl
infra/terraform.tfstate
infra/terraform.tfstate.backup
infra/terraform.tfvars
infra/backend.hcl
infra/*.tfplan

# Frontend build output
dist/

# OS
.DS_Store
